server:
  port: 81

spring:
  application:
    name: '@artifactId@'
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  task:
    execution:
      pool:
        core-size: 8
        max-size: 160
        queue-capacity: 1000
        keep-alive: 60